/* const fs = require('fs');
const btn = document.getElementById('btnText')

btn.addEventListener('click', () => {
    let textArea = document.getElementById('textMain')

    fs.writeFile("message.txt", textArea.value, function(err) {
        if(err) {
            return console.log(err);
        }
        console.log("The file was saved!");
    }); 
    
})
 */